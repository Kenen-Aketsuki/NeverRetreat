import torch
import datetime
import numpy as np
import torch.nn as nn
import torch.optim as optim
import glbSuper

class RNN_trainer:

    @staticmethod
    def train(model,xIn):
        model(xIn)
        pass